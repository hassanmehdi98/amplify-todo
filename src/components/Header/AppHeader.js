import { Col } from "reactstrap";
import Styled from "styled-components";

const AppHeader = Styled(Col)`
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 1rem;
    text-align: center;
`;

export default AppHeader;
